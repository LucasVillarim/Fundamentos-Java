package fundamentos.model;

import Interfaces.Email;
import Interfaces.Message;

public class Mobile implements Email, Message {
	String text;
	String destination;
		
	@Override
	public Object sendEmail(String message, String to) {
		this.text = message;
		this.destination = to;
		return String.format("Email to: %s; %s.", destination, text);
	}

	@Override
	public String sendMessage(String message, String to) {
		this.text = message;
		this.destination = to;
		return String.format("Message to: %s; %s.", destination, text);
	}
	
	
	
	
	
	
}
